#!/usr/bin/env sh
# Stack-specific check (typecheck + lint + test). Generated by company-agent-harness v0.3.0.
status=0

for step in "mypy ." "ruff check ." "pytest"; do
  [ -z "$step" ] && continue
  printf '\n=== %s ===\n' "$step"
  sh -c "$step" || status=1
done

exit $status
