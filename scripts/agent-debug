#!/usr/bin/env sh
# Reproduce-and-inspect helper. Generated by company-agent-harness v0.3.0.

echo "## Reproduce a failure"
echo "Run command:  python -m {{module}}"
echo "Test command: pytest"
echo
echo "1) 실패를 재현하는 최소 명령을 위에서 고른다."
echo "2) 로그를 확인하고 관련 라인을 인용한다 (docs/OBSERVABILITY.md 참고)."
echo "3) 최소 재현 케이스를 테스트로 추가한 뒤 scripts/agent-verify 로 검증한다."
