# Testing

> Generated by company-agent-harness v0.3.0.

## Commands

- Test: pytest
- Lint: ruff check .
- Typecheck: mypy .
- Build: python -m build
- Full verification: `scripts/agent-verify`

> 명령이 `_(unknown ...)_`이면 `.agent-harness.yml`의 `commands`를 채운 뒤 이 문서를 갱신한다.

## Policy

- 모든 코드 변경에는 테스트를 추가하거나 갱신한다.
- 새 기능: 동작을 검증하는 테스트를 추가한다.
- 버그 수정: 회귀를 막는 테스트를 먼저 추가한다.
- 검증을 실제로 실행하기 전에 "통과했다"고 말하지 않는다.
- 테스트 프레임워크가 없으면 사람에게 알리고 어떻게 할지 확인한다.

## How agents run tests

1. `scripts/agent-verify`를 실행한다.
2. 실패하면 출력을 그대로 보고하고 원인을 분석한다.
3. 수정 후 다시 검증한다.

## Flaky / known gaps

> Status: Unknown. 알려진 불안정 테스트나 커버리지 공백을 여기에 적는다.
