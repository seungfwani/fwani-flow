# ARCHITECTURE.md

> Generated by company-agent-harness v0.3.0. 확인된 사실과 추정을 구분해서 적는다.

## Overview

- Project type: python
- Main language: Python
- Framework: _(unknown — fill in during onboarding)_
- Source directories (detected): server, frontend
- Entry points (detected): _(unknown — fill in during onboarding)_
- Monorepo: no

> Status: 위 항목은 자동 탐지 결과다. 정확하지 않으면 사람이 직접 정정한다.

## Layers (fill in)

> Status: Unknown. 레이어 경계가 있으면 여기에 적는다. 없으면 "단일 레이어"라고 명시한다.

예시:

```
- presentation  -> application
- application   -> domain
- domain        -> (no outward dependency)
- infrastructure-> domain (구현 주입)
```

`.agent-harness.yml`의 `architecture.layers` 에도 동일하게 반영한다.

## Dependency direction (fill in)

> Status: Unknown. 허용된 의존 방향을 적는다. 에이전트는 여기에 없는 방향을 임의로 추가하면 안 된다.

## Forbidden imports (fill in)

> Status: Unknown. 금지된 import 경계를 적는다 (예: domain은 infrastructure를 import하지 않는다).

`.agent-harness.yml`의 `architecture.forbidden_imports`에 다음 형식으로 규칙을 적으면
`scripts/agent-arch-check`(= `agent-harness verify`)가 실제로 검사한다.

```yaml
architecture:
  forbidden_imports:
    - from: "src/domain"        # 이 디렉터리 아래 코드가
      pattern: "infrastructure" # import 줄에 이 문자열을 포함하면 위반
      reason: "domain must not depend on infrastructure"
```

## Rules for agents

- 이 문서에 명시되지 않은 의존성 방향은 추가하지 않는다.
- 레이어 경계가 불명확하면 먼저 이 문서를 갱신하고 사람에게 확인한다.
- 구조 변경은 `docs/exec-plans/active/`에 계획을 먼저 남긴다.
